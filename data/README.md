# Data files (missing from repo)

This folder should contain:

- `hexal_a.csv` — Control
- `hexal_b.csv` — Red button
- `hexal_c.csv` — "SEE DEALS" text
- `hexal_d.csv` — Red + "SEE DEALS"

These are referenced by `notebooks/hexal_case_solutions.ipynb` but are not
currently committed to the repository. Add them here before running the
notebook — the paths in the notebook already point to `../data/<file>.csv`.
